{
  "name": "api-platform-monorepo",
  "version": "2.2.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "build:core": "npm run build -w packages/core",
    "build:desktop": "npm run build -w apps/desktop",
    "build": "npm run build:core && npm run build:desktop",
    "package": "npm run build && npm run package -w apps/desktop",
    "package:mac": "npm run build && npm run package:mac -w apps/desktop",
    "package:win": "npm run build && npm run package:win -w apps/desktop",
    "lint": "npm run lint -w apps/desktop",
    "release:patch": "node scripts/release.mjs patch",
    "release:minor": "node scripts/release.mjs minor",
    "release:major": "node scripts/release.mjs major",
    "analyze": "node scripts/analyze-bundle.mjs"
  }
}
